@model IEnumerable<DondeComemos.Models.Restaurante>

@{
    ViewData["Title"] = "Administrar Restaurantes";
}

<<<<<<< HEAD
<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold mb-0">
                <i class="bi bi-shop"></i> Gestión de Restaurantes
            </h2>
        </div>
        <div>
            <a asp-action="Create" class="btn btn-primary btn-lg">
                <i class="bi bi-plus-circle"></i> Nuevo Restaurante
            </a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            <i class="bi bi-check-circle"></i> @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            <i class="bi bi-x-circle"></i> @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (!Model.Any())
    {
        <div class="text-center py-5">
            <i class="bi bi-inbox display-1 text-muted"></i>
            <h4 class="mt-3">No hay restaurantes registrados</h4>
            <p class="text-muted">Comienza agregando tu primer restaurante</p>
            <a asp-action="Create" class="btn btn-primary btn-lg">
                <i class="bi bi-plus-circle"></i> Agregar Primer Restaurante
            </a>
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th style="width: 80px;">Imagen</th>
                        <th>Nombre</th>
                        <th>Dirección</th>
                        <th>Tipo</th>
                        <th>Precios</th>
                        <th style="width: 100px;">Rating</th>
                        <th style="width: 120px;">Productos</th>
                        <th style="width: 300px;" class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @if (!string.IsNullOrEmpty(item.ImagenUrl))
                            {
                                <img src="@item.ImagenUrl" alt="@item.Nombre" 
                                     class="img-thumbnail" 
                                     style="width: 60px; height: 60px; object-fit: cover;" />
                            }
                            else
                            {
                                <div class="bg-light d-flex align-items-center justify-content-center" 
                                     style="width: 60px; height: 60px;">
                                    <i class="bi bi-image text-muted"></i>
                                </div>
                            }
                        </td>
                        <td>
                            <strong>@item.Nombre</strong>
                            @if (item.Destacado)
                            {
                                <br/>
                                <span class="badge bg-warning text-dark">
                                    <i class="bi bi-star-fill"></i> Destacado
                                </span>
                            }
                        </td>
                        <td>
                            <small class="text-muted">
                                <i class="bi bi-geo-alt"></i> @item.Direccion
                            </small>
                        </td>
                        <td>
                            <span class="badge bg-primary">@item.TipoCocina</span>
                        </td>
                        <td>
                            <span class="badge bg-success">@item.RangoPrecios</span>
                        </td>
                        <td>
                            <div class="text-warning">
                                @for (int i = 0; i < (int)item.Rating; i++)
                                {
                                    <i class="bi bi-star-fill"></i>
                                }
                            </div>
                            <small class="text-muted">@item.Rating.ToString("0.0")</small>
                        </td>
                        <td>
                            <a asp-controller="Productos" asp-action="Index" 
                               asp-route-restauranteId="@item.Id"
                               class="btn btn-sm btn-info w-100">
                                <i class="bi bi-menu-button-wide"></i>
                                Menú (@item.Productos.Count)
                            </a>
                        </td>
                        <td class="text-center">
                            <div class="btn-group" role="group">
                                <a asp-action="Details" asp-route-id="@item.Id" 
                                   class="btn btn-sm btn-info" title="Ver detalles">
                                    <i class="bi bi-eye"></i>
                                </a>
                                <a asp-action="Edit" asp-route-id="@item.Id" 
                                   class="btn btn-sm btn-warning" title="Editar">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <a asp-controller="Productos" asp-action="Index" 
                                   asp-route-restauranteId="@item.Id"
                                   class="btn btn-sm btn-success" title="Gestionar menú">
                                    <i class="bi bi-menu-button-wide"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id" 
                                   class="btn btn-sm btn-danger" title="Eliminar">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>

        <div class="mt-4">
            <div class="row">
                <div class="col-md-4">
                    <div class="card bg-primary text-white">
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="bi bi-shop"></i> Total Restaurantes
                            </h5>
                            <h2 class="mb-0">@Model.Count()</h2>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card bg-success text-white">
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="bi bi-menu-button-wide"></i> Total Productos
                            </h5>
                            <h2 class="mb-0">@Model.Sum(r => r.Productos.Count)</h2>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card bg-warning text-dark">
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="bi bi-star-fill"></i> Rating Promedio
                            </h5>
                            <h2 class="mb-0">@Model.Average(r => r.Rating).ToString("0.0")</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
=======
<div class="container py-4">
    <h2 class="fw-bold">🍽️ Restaurantes</h2>
    <p>
        <a asp-action="Create" class="btn btn-primary">➕ Nuevo Restaurante</a>
    </p>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(m => m.Nombre)</th>
                <th>@Html.DisplayNameFor(m => m.Direccion)</th>
                <th>@Html.DisplayNameFor(m => m.Rating)</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nombre</td>
                <td>@item.Direccion</td>
                <td>⭐ @item.Rating</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Editar</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Detalles</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Eliminar</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
<<<<<<< HEAD
</div>
=======
</div>
>>>>>>> b808e6f (Avance Mauricio Benavente)
>>>>>>> f90b87d81de3ce8c6b022ece9f01afa7f99a0eb7
