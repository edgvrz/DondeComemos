@model IEnumerable<DondeComemos.Models.Restaurante>

@{
    ViewData["Title"] = "Búsqueda";
}

<div class="container py-5">
    <h2 class="fw-semibold">🍴 Restaurantes con mejor puntuación</h2>
    <p class="text-muted">Explora y encuentra el mejor lugar para comer en Arequipa.</p>

    <div class="row">
        @foreach (var r in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm h-100">
                    <img src="@r.ImagenUrl" class="card-img-top" alt="@r.Nombre"
                         style="height:200px; object-fit:cover;" />
                    <div class="card-body">
                        <h5 class="card-title">@r.Nombre</h5>
                        <p class="card-text">@r.Direccion</p>
                        <p class="text-muted">@r.Descripcion</p>
                        <span class="fw-bold">⭐ @r.Rating / 5</span>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
