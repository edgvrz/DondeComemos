@model DondeComemos.Models.Restaurante
<<<<<<< HEAD

@{
    ViewData["Title"] = Model.Nombre;
}

<style>
    .restaurant-header {
        background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.7)), 
                    url('@Model.ImagenUrl') center/cover;
        height: 400px;
        display: flex;
        align-items: center;
        color: white;
    }
    .menu-item-card {
        transition: transform 0.3s, box-shadow 0.3s;
        border: none;
        border-radius: 15px;
    }
    .menu-item-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(0,0,0,0.15);
    }
    .menu-item-img {
        height: 200px;
        object-fit: cover;
        border-radius: 15px 15px 0 0;
    }
</style>

<!-- Header del Restaurante -->
<div class="restaurant-header">
    <div class="container">
        <div class="d-flex justify-content-between align-items-start">
            <div>
                <h1 class="display-3 fw-bold mb-3">@Model.Nombre</h1>
                <div class="d-flex align-items-center gap-4 mb-3">
                    <div class="text-warning fs-4">
                        @for (int i = 0; i < (int)Model.Rating; i++)
                        {
                            <i class="bi bi-star-fill"></i>
                        }
                        <span class="text-white ms-2">@Model.Rating.ToString("0.0")</span>
                    </div>
                    <span class="badge bg-primary fs-6">@Model.TipoCocina</span>
                    <span class="badge bg-success fs-6">@Model.RangoPrecios</span>
                </div>
                <p class="lead">
                    <i class="bi bi-geo-alt"></i> @Model.Direccion
                </p>
            </div>
            
            @if (User.Identity?.IsAuthenticated ?? false)
            {
                <div>
                    @if (User.Identity?.IsAuthenticated ?? false)
{
    <div class="d-flex gap-2">
        <a asp-controller="Reservas" asp-action="Create" 
           asp-route-restauranteId="@Model.Id"
           class="btn btn-success btn-lg">
            <i class="bi bi-calendar-plus"></i> Hacer Reserva
        </a>
        
        <form asp-controller="Favoritos" asp-action="Agregar" method="post" id="favoritoForm">
            <input type="hidden" name="restauranteId" value="@Model.Id" />
            <button type="submit" class="btn btn-light btn-lg" id="btnFavorito">
                <i class="bi bi-heart" id="iconFavorito"></i>
                <span id="textoFavorito">Favoritos</span>
            </button>
        </form>
    </div>
}
                </div>
            }
=======
<<<<<<< HEAD

@{
    ViewData["Title"] = "Detalles del Restaurante";
}

<div class="container py-4">
    <h2 class="fw-bold">📖 Detalles</h2>

    <dl class="row">
        <dt class="col-sm-2">Nombre</dt>
        <dd class="col-sm-10">@Model.Nombre</dd>

        <dt class="col-sm-2">Dirección</dt>
        <dd class="col-sm-10">@Model.Direccion</dd>

        <dt class="col-sm-2">Descripción</dt>
        <dd class="col-sm-10">@Model.Descripcion</dd>

        <dt class="col-sm-2">Rating</dt>
        <dd class="col-sm-10">⭐ @Model.Rating</dd>
    </dl>

    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Editar</a>
    <a asp-action="Index" class="btn btn-secondary">Volver</a>
</div>
=======
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@{
    ViewData["Title"] = Model.Nombre;
    var userId = UserManager.GetUserId(User);
    var yaReseno = Model.Resenas.Any(r => r.UserId == userId);
}

<style>
    .restaurant-hero {
        position: relative;
        height: 400px;
        background: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.5)), 
                    url('@(string.IsNullOrEmpty(Model.ImagenUrl) ? "/img/restaurant-default.jpg" : Model.ImagenUrl)') center/cover;
        color: white;
        display: flex;
        align-items: flex-end;
        border-radius: 15px;
        overflow: hidden;
        margin-bottom: 30px;
    }
    
    .hero-content {
        padding: 40px;
        width: 100%;
        background: linear-gradient(transparent, rgba(0,0,0,0.8));
    }
    
    .star-rating {
        color: #ffc107;
        font-size: 1.5rem;
    }
    
    .product-card {
        transition: all 0.3s ease;
        height: 100%;
    }
    
    .product-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(0,0,0,0.15) !important;
    }
    
    .rating-slider-input {
        width: 100%;
    }
    
    .rating-display {
        text-align: center;
        font-size: 2rem;
        color: #ffc107;
        font-weight: bold;
    }
</style>

<div class="restaurant-hero">
    <div class="hero-content">
        <div class="d-flex justify-content-between align-items-end">
            <div>
                <h1 class="display-4 fw-bold mb-2">@Model.Nombre</h1>
                <div class="mb-2">
                    @if (!string.IsNullOrEmpty(Model.TipoCocina))
                    {
                        <span class="badge bg-light text-dark me-2">@Model.TipoCocina</span>
                    }
                    @if (!string.IsNullOrEmpty(Model.RangoPrecios))
                    {
                        <span class="badge bg-success">@Model.RangoPrecios</span>
                    }
                </div>
                <div class="star-rating mb-2">
                    @{
                        var fullStars = (int)Math.Floor(Model.RatingPromedio);
                        var hasHalfStar = (Model.RatingPromedio - fullStars) >= 0.5;
                        var emptyStars = 5 - fullStars - (hasHalfStar ? 1 : 0);
                    }
                    @for (int i = 0; i < fullStars; i++)
                    {
                        <i class="bi bi-star-fill"></i>
                    }
                    @if (hasHalfStar)
                    {
                        <i class="bi bi-star-half"></i>
                    }
                    @for (int i = 0; i < emptyStars; i++)
                    {
                        <i class="bi bi-star"></i>
                    }
                    <span class="ms-2 fs-5">@Model.RatingPromedio.ToString("F1") (@Model.TotalResenas reseñas)</span>
                </div>
                <p class="mb-0"><i class="bi bi-geo-alt"></i> @Model.Direccion</p>
            </div>
            <div class="text-end">
                @if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
                {
                    <a asp-controller="Productos" asp-action="Index" asp-route-restauranteId="@Model.Id" 
                       class="btn btn-light me-2">
                        <i class="bi bi-gear"></i> Gestionar Menú
                    </a>
                }
                <button class="btn btn-danger" onclick="exportarPDF()">
                    <i class="bi bi-file-pdf"></i> Exportar Menú
                </button>
            </div>
>>>>>>> f90b87d81de3ce8c6b022ece9f01afa7f99a0eb7
        </div>
    </div>
</div>

<<<<<<< HEAD
@section Scripts {
    <script>
        // Verificar si el restaurante es favorito
        @if (User.Identity?.IsAuthenticated ?? false)
        {
            <text>
            fetch('/Favoritos/EsFavorito?restauranteId=@Model.Id')
                .then(response => response.json())
                .then(data => {
                    if (data.esFavorito) {
                        document.getElementById('iconFavorito').className = 'bi bi-heart-fill text-danger';
                        document.getElementById('textoFavorito').textContent = 'En Favoritos';
                        document.getElementById('btnFavorito').classList.add('active');
                    }
                });
            </text>
        }
    </script>
}

<div class="container py-5">
    <div class="row g-4">
        <!-- Información del Restaurante -->
        <div class="col-lg-4">
            <div class="card shadow-sm border-0 sticky-top" style="top: 20px;">
                <div class="card-body p-4">
                    <h4 class="mb-3">
                        <i class="bi bi-info-circle text-primary"></i> Información
                    </h4>

                    <div class="mb-3">
                        <h6 class="text-muted mb-2">Descripción</h6>
                        <p>@Model.Descripcion</p>
                    </div>

                    @if (!string.IsNullOrEmpty(Model.Telefono))
                    {
                        <div class="mb-3">
                            <h6 class="text-muted mb-2">
                                <i class="bi bi-telephone"></i> Teléfono
                            </h6>
                            <p><a href="tel:@Model.Telefono">@Model.Telefono</a></p>
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model.Horario))
                    {
                        <div class="mb-3">
                            <h6 class="text-muted mb-2">
                                <i class="bi bi-clock"></i> Horario
                            </h6>
                            <p>@Model.Horario</p>
                        </div>
                    }

                    <div class="mb-3">
                        <h6 class="text-muted mb-2">
                            <i class="bi bi-gear"></i> Servicios
                        </h6>
                        <div class="d-flex flex-wrap gap-2">
                            @if (Model.DeliveryDisponible)
                            {
                                <span class="badge bg-success">
                                    <i class="bi bi-truck"></i> Delivery
                                </span>
                            }
                            @if (Model.AceptaReservas)
                            {
                                <span class="badge bg-primary">
                                    <i class="bi bi-calendar-check"></i> Reservas
                                </span>
                            }
                            @if (Model.TieneEstacionamiento)
                            {
                                <span class="badge bg-info">
                                    <i class="bi bi-car-front"></i> Estacionamiento
                                </span>
                            }
                            @if (Model.WifiGratis)
                            {
                                <span class="badge bg-secondary">
                                    <i class="bi bi-wifi"></i> WiFi
                                </span>
                            }
                            @if (Model.OpcionesVegetarianas)
                            {
                                <span class="badge bg-success">
                                    <i class="bi bi-leaf"></i> Vegetariano
                                </span>
                            }
                            @if (Model.OpcionesVeganas)
                            {
                                <span class="badge bg-success">
                                    <i class="bi bi-flower1"></i> Vegano
                                </span>
                            }
                        </div>
                    </div>

                    @if (!string.IsNullOrEmpty(Model.GoogleMapsUrl))
                    {
                        <div class="d-grid gap-2 mb-3">
                            <a href="@Model.GoogleMapsUrl" target="_blank" 
                               class="btn btn-primary">
                                <i class="bi bi-map"></i> Ver en Google Maps
                            </a>
                        </div>
                    }

                    <!-- Redes Sociales -->
                    <div class="d-flex gap-2">
                        @if (!string.IsNullOrEmpty(Model.SitioWeb))
                        {
                            <a href="@Model.SitioWeb" target="_blank" 
                               class="btn btn-outline-secondary btn-sm">
                                <i class="bi bi-globe"></i>
                            </a>
                        }
                        @if (!string.IsNullOrEmpty(Model.Facebook))
                        {
                            <a href="@Model.Facebook" target="_blank" 
                               class="btn btn-outline-primary btn-sm">
                                <i class="bi bi-facebook"></i>
                            </a>
                        }
                        @if (!string.IsNullOrEmpty(Model.Instagram))
                        {
                            <a href="@Model.Instagram" target="_blank" 
                               class="btn btn-outline-danger btn-sm">
                                <i class="bi bi-instagram"></i>
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>

        <!-- Menú del Restaurante -->
        <div class="col-lg-8">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h3 class="fw-bold">
                    <i class="bi bi-menu-button-wide text-primary"></i> Nuestro Menú
                </h3>
                @if (User.IsInRole("Admin"))
                {
                    <a asp-controller="Productos" asp-action="Create" 
                       asp-route-restauranteId="@Model.Id"
                       class="btn btn-success">
                        <i class="bi bi-plus-circle"></i> Agregar Producto
                    </a>
                }
            </div>

            @if (!Model.Productos.Any())
            {
                <div class="alert alert-info text-center">
                    <i class="bi bi-info-circle fs-1 d-block mb-3"></i>
                    <h5>Aún no hay productos en el menú</h5>
                    @if (User.IsInRole("Admin"))
                    {
                        <p>Comienza agregando los platos que ofrece este restaurante</p>
                        <a asp-controller="Productos" asp-action="Create" 
                           asp-route-restauranteId="@Model.Id"
                           class="btn btn-primary">
                            <i class="bi bi-plus-circle"></i> Agregar Primer Producto
                        </a>
                    }
                </div>
            }
            else
            {
                @foreach (var categoria in Model.Productos.GroupBy(p => p.Categoria))
                {
                    <div class="mb-5">
                        <h4 class="border-bottom pb-2 mb-4">
                            <i class="bi bi-bookmark-fill text-primary"></i> @categoria.Key
                        </h4>

                        <div class="row g-4">
                            @foreach (var producto in categoria.OrderBy(p => p.Orden))
                            {
                                <div class="col-md-6">
                                    <div class="card menu-item-card shadow-sm h-100">
                                        @if (!string.IsNullOrEmpty(producto.ImagenUrl))
                                        {
                                            <img src="@producto.ImagenUrl" 
                                                 class="menu-item-img" 
                                                 alt="@producto.Nombre" />
                                        }
                                        else
                                        {
                                            <div class="menu-item-img bg-light d-flex align-items-center justify-content-center">
                                                <i class="bi bi-image display-4 text-muted"></i>
                                            </div>
                                        }
                                        
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start mb-2">
                                                <h5 class="card-title mb-0">@producto.Nombre</h5>
                                                @if (producto.RecomendacionChef)
                                                {
                                                    <span class="badge bg-warning text-dark">
                                                        <i class="bi bi-star-fill"></i>
                                                    </span>
                                                }
                                            </div>

                                            @if (!string.IsNullOrEmpty(producto.Descripcion))
                                            {
                                                <p class="card-text text-muted small">
                                                    @producto.Descripcion
                                                </p>
                                            }

                                            <div class="d-flex flex-wrap gap-1 mb-2">
                                                @if (producto.EsVegetariano)
                                                {
                                                    <span class="badge bg-success" title="Vegetariano">
                                                        <i class="bi bi-leaf"></i>
                                                    </span>
                                                }
                                                @if (producto.EsVegano)
                                                {
                                                    <span class="badge bg-success" title="Vegano">
                                                        <i class="bi bi-flower1"></i>
                                                    </span>
                                                }
                                                @if (producto.SinGluten)
                                                {
                                                    <span class="badge bg-info" title="Sin Gluten">
                                                        <i class="bi bi-shield-check"></i>
                                                    </span>
                                                }
                                                @if (producto.Picante)
                                                {
                                                    <span class="badge bg-danger" title="Picante">
                                                        <i class="bi bi-fire"></i>
                                                    </span>
                                                }
                                            </div>

                                            <div class="d-flex justify-content-between align-items-center">
                                                <h4 class="text-success mb-0">
                                                    S/ @producto.Precio.ToString("0.00")
                                                </h4>
                                                @if (producto.TiempoPreparacion.HasValue)
                                                {
                                                    <small class="text-muted">
                                                        <i class="bi bi-clock"></i> @producto.TiempoPreparacion min
                                                    </small>
                                                }
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                }
            }
        </div>
    </div>

    <!-- Reseñas -->
    <div class="row mt-5">
        <div class="col-12">
            <h3 class="fw-bold mb-4">
                <i class="bi bi-chat-left-text text-primary"></i> Reseñas
            </h3>

            @if (!Model.Resenas.Any())
            {
                <div class="alert alert-info text-center">
                    <i class="bi bi-info-circle fs-1 d-block mb-3"></i>
                    <h5>Aún no hay reseñas</h5>
                    <p>¡Sé el primero en compartir tu experiencia!</p>
                </div>
            }
            else
            {
                <div class="row g-4">
                    @foreach (var resena in Model.Resenas.Take(6))
                    {
                        <div class="col-md-6">
                            <div class="card shadow-sm">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between mb-2">
                                        <h5 class="mb-0">@resena.Titulo</h5>
                                        <div class="text-warning">
                                            @for (int i = 0; i < (int)resena.Calificacion; i++)
                                            {
                                                <i class="bi bi-star-fill"></i>
                                            }
                                        </div>
                                    </div>
                                    <p class="card-text">@resena.Comentario</p>
                                    <small class="text-muted">
                                        @resena.FechaCreacion.ToString("dd/MM/yyyy")
                                    </small>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            }
        </div>
    </div>
</div>
=======
<div class="container-fluid">
    @if (SignInManager.IsSignedIn(User) && !yaReseno)
    {
        <div class="alert alert-info mb-4">
            <h5><i class="bi bi-pencil-square"></i> Deja tu reseña</h5>
            <form asp-controller="Resenas" asp-action="Create" method="post">
                <input type="hidden" name="RestauranteId" value="@Model.Id" />
                
                <div class="row">
                    <div class="col-md-12 mb-3">
                        <label class="form-label fw-bold">Calificación General</label>
                        <div class="card bg-light p-3">
                            <input type="range" class="form-range rating-slider-input" 
                                   min="0.5" max="5.0" step="0.1" 
                                   name="Calificacion" id="calificacionGeneral" value="5.0" 
                                   oninput="updateRatingDisplay('calificacionGeneral', 'ratingDisplay', this.value)">
                            <div class="rating-display" id="ratingDisplay">5.0 ⭐</div>
                        </div>
                    </div>
                </div>
                
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">Calidad de Comida (0.5-5.0)</label>
                        <input type="range" class="form-range" min="0.5" max="5.0" step="0.1" 
                               name="CalidadComida" value="5.0" 
                               oninput="this.nextElementSibling.textContent = this.value + ' ⭐'">
                        <small class="d-block text-center">5.0 ⭐</small>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Servicio (0.5-5.0)</label>
                        <input type="range" class="form-range" min="0.5" max="5.0" step="0.1" 
                               name="Servicio" value="5.0"
                               oninput="this.nextElementSibling.textContent = this.value + ' ⭐'">
                        <small class="d-block text-center">5.0 ⭐</small>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Ambiente (0.5-5.0)</label>
                        <input type="range" class="form-range" min="0.5" max="5.0" step="0.1" 
                               name="Ambiente" value="5.0"
                               oninput="this.nextElementSibling.textContent = this.value + ' ⭐'">
                        <small class="d-block text-center">5.0 ⭐</small>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Relación Calidad-Precio (0.5-5.0)</label>
                        <input type="range" class="form-range" min="0.5" max="5.0" step="0.1" 
                               name="RelacionPrecio" value="5.0"
                               oninput="this.nextElementSibling.textContent = this.value + ' ⭐'">
                        <small class="d-block text-center">5.0 ⭐</small>
                    </div>
                </div>
                
                <div class="mb-3">
                    <label class="form-label fw-bold">Título de tu reseña</label>
                    <input type="text" class="form-control" name="Titulo" required 
                           placeholder="Ej: Excelente experiencia gastronómica">
                </div>
                
                <div class="mb-3">
                    <label class="form-label fw-bold">Tu comentario</label>
                    <textarea class="form-control" name="Comentario" rows="4" required
                              placeholder="Cuéntanos sobre tu experiencia..."></textarea>
                </div>
                
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-send"></i> Publicar Reseña
                </button>
            </form>
        </div>
    }
    
    <a asp-action="Search" class="btn btn-secondary mb-4">
        <i class="bi bi-arrow-left"></i> Volver a la búsqueda
    </a>
</div>

@section Scripts {
    @if (Model.Latitud.HasValue && Model.Longitud.HasValue)
    {
        <script src="https://maps.googleapis.com/maps/api/js?key=TU_API_KEY_AQUI"></script>
        <script>
            function initMap() {
                const position = { lat: @Model.Latitud.Value, lng: @Model.Longitud.Value };
                const map = new google.maps.Map(document.getElementById("map"), {
                    zoom: 16,
                    center: position,
                });
                new google.maps.Marker({
                    position: position,
                    map: map,
                    title: "@Model.Nombre"
                });
            }
            window.onload = initMap;
        </script>
    }
    
    <script>
        function updateRatingDisplay(inputId, displayId, value) {
            const displayValue = parseFloat(value).toFixed(1);
            document.getElementById(displayId).textContent = displayValue + ' ⭐';
        }
        
        function exportarPDF() {
            window.open('/Restaurantes/ExportarMenuPdf/@Model.Id', '_blank');
        }
    </script>
}
>>>>>>> b808e6f (Avance Mauricio Benavente)
>>>>>>> f90b87d81de3ce8c6b022ece9f01afa7f99a0eb7
