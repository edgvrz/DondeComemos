@model IEnumerable<DondeComemos.Models.Restaurante>

@{
    ViewData["Title"] = "Explorar Restaurantes";
}

<div class="container py-4">
    <h2 class="fw-bold">🍴 Explorar Restaurantes</h2>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(m => m.Nombre)</th>
                <th>@Html.DisplayNameFor(m => m.Direccion)</th>
                <th>@Html.DisplayNameFor(m => m.Rating)</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@(item.Nombre ?? "Nombre no disponible")</td>
                    <td>@(item.Direccion ?? "Dirección no disponible")</td>
                    <td>⭐ @item.Rating</td>
                    <td>
                        @if (User.Identity != null && User.Identity.IsAuthenticated)
                        {
                            <form asp-action="Add" asp-controller="Watchlist" method="post" style="display:inline;">
                     @Html.AntiForgeryToken()
                        <input type="hidden" name="restauranteId" value="@item.Id" />
                         <button type="submit" class="btn btn-warning btn-sm">
                        ⭐ Agregar a Favoritos
             </button>
            </form>
                        }
                        else
                        {
                            <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-warning btn-sm">⭐ Inicia sesión para guardar</a>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
